= form_for @picture, :html => { :id => "#new_picture" }, :remote => true, :method => :post do |f|
  = f.file_field :image, :class => "upload-profile-photo-field"
  .col-md-12.text-right
    = f.submit "Upload", :class => "upload-profile-photo-submit", :class => "btn btn-default"