<% if activity.owner.id == @current_user.id %>
	You paid for you
<% else %>
	<%= link_to "#{activity.owner.first_name} #{activity.owner.last_name.truncate(2, omission: '.')}", activity.owner if activity.owner %> paid you for their
<% end %>

reservation at <%= link_to "#{activity.trackable.reservation.listing.name}", listings_path %>.